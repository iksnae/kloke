package assets {				import fl.controls.Button;	import fl.controls.DataGrid;	import fl.controls.TextInput;	import fl.controls.dataGridClasses.DataGridColumn;	import fl.data.DataProvider;		import flash.display.MovieClip;		public class VideoBrowser extends MovieClip{				// Constants:		// Public Properties:		// Private Properties:		// UI Elements:		public var data_grid:DataGrid;		public var search_btn:Button;		public var search_txt:TextInput;						///		private var titleColumn:DataGridColumn = new DataGridColumn("title")		private var dateColumn:DataGridColumn = new DataGridColumn("date")        private var viewsColumn:DataGridColumn = new DataGridColumn("views")        private var rankColumn:DataGridColumn= new DataGridColumn("rank")                						// Initialization:		public function VideoBrowser() {			configUI()		}		// Public Methods:		// Protected Methods:		// Private Methods:		protected function configUI():void { 						viewsColumn.headerText	= 'views';			viewsColumn.width = 50;			viewsColumn.resizable = false									titleColumn.headerText   = 'title';			titleColumn.headerRenderer = assets.VideoHeaderRenderer            titleColumn.sortable = false            titleColumn.cellRenderer = assets.VideoTitleCellRenderer            dateColumn.headerText  = 'date';            dateColumn.width = 50;                      rankColumn.headerText  = 'rank';            rankColumn.width = 50;                        data_grid.rowHeight = 90;		   // data_grid.addColumn(viewsColumn)		    data_grid.addColumn(titleColumn); 		   // data_grid.addColumn(dateColumn)		   // data_grid.addColumn(rankColumn)            			fakeData()		}						private function fakeData():void{			trace('fakeData')			var dp:DataProvider = new DataProvider()			for(var i:int=0;i<30;i++){				data_grid.addItem({title:'title',value:'value'})											}				}	}	}